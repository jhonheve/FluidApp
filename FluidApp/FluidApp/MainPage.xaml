<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FluidApp"
             x:Class="FluidApp.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <Image x:Name="LoadedPicture" WidthRequest="250" Source="{Binding DocumentResource}" />
            <Button Text="Take The picture of your document Identity" x:Name="btIdentification"
                    Command="{Binding LoadDocumentHandler}" />
            
            <Button Text="Start the process" 
                x:Name="Next" HorizontalOptions="Fill" VerticalOptions="End" Font="20"
                Command="{Binding NextSelfiePageHandler}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
